import React from "react";
import { StyleSheet, Text, View, SafeAreaView  } from "react-native";
import tw from 'twrnc';

const HomeScreen = () => {
    return( 
        <SafeAreaView >
            <Text>Eu sou a tela Inicial </Text>
        </SafeAreaView>
    );
};

export default HomeScreen;

const styles = StyleSheet.create({
    text:{
        color: "blue",
    }

});
  

